# NORE-AI Configs

This directory holds configuration files used by higher-level pipelines.

Current:
- `global.json` — default paths, schema locations, and runtime feature flags.

Future:
- `weekly.json` — options for weekly rollups
- `cycle.json` — options for cycle-window analysis
- `register.json` — writing register outputs (daily/weekly/cycle)
